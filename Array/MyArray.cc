class MyArray
{
    int size;
    int capacity;
    public:
        int size();
        int capacity();
        bool is_empty();
        int at(index);
        void push(item);
        void insert(index, item);
        int pop();
        void deletey(index);
        void remove(item);
        void find(item);
        this resize(new_capacity);
    
    }
